Entity sets are naturally translated to table schemas, where every attribute in an entity set corresponds to an attribute in the table schema. However, as we have noted in section \ref{sec:rdbschema}, not every attribute type is supported in database schemas.

%if False

> module Basil.Database.Relational.Entities where

%endif

TODO
